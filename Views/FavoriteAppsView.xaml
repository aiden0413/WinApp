<UserControl x:Class="winapp.FavoriteAppsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:winapp"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="700">
    
    <Border Background="#222222" BorderBrush="#2D2D2D" BorderThickness="1" CornerRadius="8" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 상단 타이틀 및 [삭제 모드] 토글 버튼 -->
            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="즐겨찾기 앱" FontSize="14" FontWeight="Bold" Foreground="#DDDDDD" VerticalAlignment="Center"/>
                <Button x:Name="BtnToggleDeleteMode" Content="삭제 모드" Width="75" Height="26" Background="#333333" Foreground="#CCCCCC" BorderBrush="#444444"
                        HorizontalAlignment="Right" Click="BtnToggleDeleteMode_Click" Cursor="Hand" FontSize="11">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#444444"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </DockPanel>

            <!-- 120x120 정사각형 카드 그리드 -->
            <ListBox x:Name="FavoriteListBox" Grid.Row="1" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Width" Value="120"/>
                        <Setter Property="Height" Value="120"/>
                        <Setter Property="Background" Value="#2A2A2A"/>
                        <Setter Property="BorderBrush" Value="#383838"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Margin" Value="0,0,10,10"/>
                        <Setter Property="Padding" Value="8"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#353535"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="104" Height="104">
                            <!-- 1. 일반 앱 카드 (아이콘 크기 56x56으로 확대) -->
                            <Grid x:Name="AppPanel">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Image Source="{Binding AppIcon}" Width="56" Height="56" Margin="0,0,0,6" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                                    <TextBlock Text="{Binding AppName}" Foreground="White" FontSize="11" TextAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="100"/>
                                </StackPanel>

                                <!-- 삭제 모드가 켜질 때만 나타나는 우측 상단 X 버튼 -->
                                <Button x:Name="DeleteButton" Content="✕" Width="22" Height="22" HorizontalAlignment="Right" VerticalAlignment="Top"
                                        Background="#555555" Foreground="White" BorderThickness="0" Cursor="Hand"
                                        Click="BtnDelete_Click" Tag="{Binding}" Visibility="Collapsed">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="11">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#CC3333"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>

                            <!-- 2. [+] 앱 추가 버튼 카드 -->
                            <StackPanel x:Name="AddPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                                <TextBlock Text="+" FontSize="32" Foreground="#888888" TextAlignment="Center" Margin="0,0,0,4"/>
                                <TextBlock Text="앱 추가" FontSize="12" Foreground="#AAAAAA" TextAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                        
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsAddButton}" Value="True">
                                <Setter TargetName="AppPanel" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="AddPanel" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>